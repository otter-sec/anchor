[package]
name = "registry"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "registry"

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = { package = "anchor-lang-v1", path = "../../../../lang" }
anchor-spl = { package = "anchor-spl-v1", path = "../../../../spl" }
lockup = { path = "../lockup", features = ["cpi"] }
